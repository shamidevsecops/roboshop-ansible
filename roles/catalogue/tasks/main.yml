- name: Load NodeJS
  ansible.builtin.import_role:
    name: common
    tasks_from: nodejs

- name: Copy MongoDB repo file
  ansible.builtin.copy:
    src: mongo.repo
    dest: /etc/yum.repos.d/mongo.repo

- name: Load Schema
  ansible.builtin.shell: mongosh --host mongodb-dev.shamidevops.online </app/db/master-data.js